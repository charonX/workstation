# Test-as-Contract workflow state
# 由 /story 创建和维护。本文件是状态机,不归档(回流时更新,不搬进 archive/)。

story_id: 2026-08-16-deepen-db-per-path-cache
intention: >-
  全局单槽连接按路径切换即关旧库：每个模块被迫「每次操作重新取」并堆防御注释，
  跨库 join 全部 JS 侧手工归并，缓存一次句柄即埋 database-not-open。
  一句话痛点：连接生命周期是全局隐式状态，正确用法靠注释与纪律维持。   # 一句话痛点,非方案。这是 story 的初衷,方案会变,痛点不会。

phase: TEST        # THINK / PRD / DESIGN / DOMAIN-MODEL / TECH-DESIGN(仅 complex；simple 直接结晶) / CRYSTALLIZE / TEST / ASSERTION-SIGNOFF / BUILD / QA / BUG / REFLECT
attempt: 1        # 第几次尝试。归档重做时 +1。
bug-counter: 0    # story 内 bug 计数，/bug 每次修复 +1，用于 commit [bugfix] BUG-NNN。
created: 2026-08-16

# 阶段流转历史。正常前进和回流都记一条。
history:
  - {at: 2026-08-16, from: -, to: THINK, note: "创建 story（/improve-codebase-architecture 评审候选 #5，报告存档 .aiassist/global/architecture-reviews/architecture-review-2026-08-16.html）"}
  - {at: 2026-08-18, from: THINK, to: PRD, note: "需求洞察完成（interview-notes.md）：勘察 55 处 getDb 调用横跨 20+ 模块、单槽切路径即关旧库、closeDb 只关单连接、:memory: 共享语义。四问全确认（用户拍板）：①缓存生命周期=进程+closeDb 关全部 ②范围=只改 db.js+最少必要调用点，注释删除逐模块 ③:memory:=缓存但 closeDb 清全部 ④API=透明替换 getDb 签名不变。方向锁定：per-path 连接缓存（Outcome 双库并存句柄可缓存 / Out of scope 55 点全量重构+跨库 join+LRU）"}
  - {at: 2026-08-18, from: PRD, to: CRYSTALLIZE, note: "PRD v1 定稿（人确认）：B1 per-path 缓存 / B2 closeDb 语义升级（关全部+定向）/ B3 最少必要调用点清理三稳定块；simple（§10 高层完整，单模块透明替换零调用点改动）；锚点 6 行全可机器断言。UX 面 N/A（纯后端连接生命周期重构），DESIGN 跳过；DOMAIN-MODEL 无新术语，跳过。进结晶"}
  - {at: 2026-08-18, from: CRYSTALLIZE, to: TEST, note: "结晶 3 REQ（延续 workspace-management/server 域编号 REQ-WORKSPACE-014~016）：014 per-path 缓存（AC1-AC5 同句柄/并存/句柄可持有/默认路径）、015 closeDb 升级（AC1-AC5 关全部/定向/no-op/:memory:/resetDb 保持）、016 最少调用点清理（AC1-AC2 全量回归零破坏）。requirements-v1.hash=db8799ff；能力地图 server 行追加。无升级点。自动链续跑 /test-author"}

# 归档记录。每次"归档重做"追加一条;删 story 不记(整个目录删除)。
archive: []
#  - {attempt: 1, date: 2026-07-03, reason_file: archive/attempt-1/reason.md, root_layer: PRD}
#    root_layer 取值:THINK / PRD / DESIGN / DOMAIN-MODEL / TECH-DESIGN —— 表示这次回流判定根因活在哪一层。
#    TECH-DESIGN 语义 = 重做 prd.md §10 技术方案（仅 complex story）。

# 块状态(一挡内按块回流时维护)。
blocks: {}
#  stable: [块A, 块B]      # 已稳定,可结晶为 REQ
#  moving: [块C]           # 还在动,暂不入 REQ(被推翻的块降级回这里)

# 设计系统绑定(由 /design-system 与 /ux-explore 维护)。
design_system:
  slug: ""                  # 主设计系统 slug,对应 _d_meta.json 的 primaryDesignSystem
  compiled: false           # 是否已跑过 compile-design-system
  preview: ""               # 当前有效的 preview.html 路径

# story 内已记录的设计资产(由 record-asset.mjs 维护)。
assets:
  recorded: []              # [flow-a.html, flow-b.html, ...]

# 已生成的变体(按需)。
variants:
  active: []                # [dark, compact, ...]
