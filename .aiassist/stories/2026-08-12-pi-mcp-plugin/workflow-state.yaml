# Test-as-Contract workflow state
# 由 /story 创建和维护。本文件是状态机,不归档(回流时更新,不搬进 archive/)。

story_id: 2026-08-12-pi-mcp-plugin
intention: >-
  PI agent 能力受限：无法通过 MCP 调用外部工具（如 Linear/Notion/数据库/内部服务），
  也缺少应用内的 PI 插件安装/配置管理。一句话痛点：agent 能力受限，没有「安装 -> 配置 -> 使用」的扩展链路。

phase: BUG     # THINK / PRD / DESIGN / DOMAIN-MODEL / TECH-DESIGN / CRYSTALLIZE / TEST / ASSERTION-SIGNOFF / BUILD / QA / BUG / REFLECT
attempt: 1        # 第几次尝试。归档重做时 +1。
bug-counter: 10    # story 内 bug 计数，/bug 每次修复 +1，用于 commit [bugfix] BUG-NNN。
created: 2026-08-12

# 阶段流转历史。正常前进和回流都记一条。
history:
  - {at: 2026-08-12, from: -, to: THINK, note: "创建 story；范围：MCP 支持 + PI 插件配置/安装管理，同一 story 一起做"}
  - {at: 2026-08-12, from: THINK, to: THINK, note: "需求洞察完成（interview-notes.md，方向 A：插件机制+MCP 桥随舰）；人拍板先 /research 再 /to-prd；根因伏笔：技术可行性层"}
  - {at: 2026-08-12, from: THINK, to: PRD, note: "双调研完成（社区桥 pi-mcp-adapter 可用；官方包机制程序化可达）；PRD v0.1 初稿待审查，complex"}
  - {at: 2026-08-12, from: PRD, to: TECH-DESIGN, note: "6 轮对抗式深潜定稿：A 全量转官方 / B1 桥内置内联 / C1 .pi 文件启用 / D1 gotgenes mcp 面 / 缺包报错 / 项目默认信任 / E1 pi 0.84.1 前置（B9）/ MCP 全量 transport；PRD 反向同步 v0.2；ADR-024/025 落盘"}
  - {at: 2026-08-12, from: TECH-DESIGN, to: CRYSTALLIZE, note: "结晶 REQ-AGENT-078~090（13 条，新能力 plugin-management，实体 extension/mcp-server）；requirements-v1.hash 落盘；能力地图更新"}
  - {at: 2026-08-12, from: CRYSTALLIZE, to: DESIGN, note: "人指出需补 DESIGN：管理区插件页/MCP 表单/权限 mcp 分组出 ux 原型；REQ-083/087 的 UX 参照待回填"}
  - {at: 2026-08-13, from: DESIGN, to: TEST, note: "ux 三原型定稿（plugins-page/permission-mcp-group/oauth-present，人确认）；M2 消解为弹窗常驻告知条；REQ-083/087 回填 UX 参照并重算 hash；进 TEST（/test-author）"}
  - {at: 2026-08-13, from: TEST, to: ASSERTION-SIGNOFF, note: "测试骨架 9 文件 + 3 fixture 落盘（全 RED：seam 未就绪/CLI 缺命令/golden 缺 mcp 族）；test-plan.md 列出 6 个待签核断言点；待门 1"}
  - {at: 2026-08-13, from: ASSERTION-SIGNOFF, to: BUILD, note: "门 1 通过：D1-D7 人批量签核（signoff.md）；48 处 TODO 落签；43 用例全 RED 无误绿；解锁 BUILD（第 0 切片 = REQ-078 pi 0.84.1 升级）"}
  - {at: 2026-08-13, from: BUILD, to: BUG, note: "BUILD 6/6 切片完成（782 pass / 2 fail）；进入 bug 处理。BUG-001（test-gap）：mcpService 标准 5 import(pi-mcp-adapter) 与 TS-source 包在 Node 24 不兼容 → 改 jiti，5/5 绿，commit c44d04d [test]"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-002（code-defect，人确认）：npm start 主进程 bundle 崩（App threw an error during load）——Slice 4 plugins/mcp 路由使主 bundle 首次引入 extensionService→pi-coding-agent，且 modelCatalogService→pi-ai/providers/all subpath、flowEngine→claude-agent-sdk；这些 CJS require(child_process/async_hooks) 内联进 ESM bundle 崩。修 vite.main.config.js external（regex 覆盖 pi 系列 + claude-agent-sdk，对齐 worker），冒烟验证应用加载正常，commit 14673a7 [bugfix]"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-003（test-gap）：pluginsPage E2E 标准 3/4 缺 seed → 补 API seed（已装插件+demo 项目+坏插件），E2E 6/8→8/8（真实 Electron 启动验证），commit 7d7a6ee [test]"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-004（test-gap）：mcpBridge 4 空断言 → 接真实全链路（stdio/http/快照隔离/新会话生效，FAUX 驱动 + fixture 调用日志断言），4/4 真实断言绿，commit a84c107 [test]。全量回归 784/783/1（唯一 fail=sessionMessage 并行 story churn）"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-005（code-defect）：agentDir 真源统一为 <cwd>/.agent-home（HTTP 层与 worker OPC_AGENT_HOME 同源；保留测试隔离/显式注入），commit eb6101a [bugfix]。三处 REFLECT 裁决完成：飞书走通用空间✓；B1 装配层过滤 ADR-024 姿态补充✓；agentDir 真源 A✓"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-006（req-gap，人确认+拍板「表单输入+secretStore 加密」）：bearer token 无录入点——REQ-084 契约/已批准 HTML/实现三层一致地漏 token 字段。就地补全（不退 phase）：REQ 契约+AC6（d7dab13 [docs]）、UX 加 token 输入框+重生成 preview（dd50033 [ux]）、回归测试 6a/6b/6c+E2E 先红（3bd6022 [test]）、mcpBridge 标准4 契约适配（9ba0b99 [test]）、实现 4943dfd [bugfix]：secretStore 加密落 token_enc 列→快照解密映射桥 bearerToken。单测全绿+E2E 9/9；唯一预存 fail=sessionMessage 409（干净 HEAD 复现，非本 bug，疑 <cwd>/.agent-home 残留致'未配置'前提失效）"}
  - {at: 2026-08-14, from: BUG, to: BUG, note: "BUG-007（test-gap，人确认）：BUG-006 尾注的预存 fail 根治——sessionMessage「agent 未配置→409」实际是同进程 describe 间污染：前一 describe configureAgent 落盘共享临时 configDir 的 settings.json，ADR-026 slice-1（1dfeff8）后 resetSettings E13『磁盘为真相不覆盖』使后一 describe 的『未配置』前提失效（.agent-home 假设实证排除；单跑全绿/连跑必红）。修测试侧 beforeEach 显式删 settings.json+重载默认，断言不动（0002a62 [test]）。全量单元 847/847 绿"}
  - {at: 2026-08-15, from: BUG, to: BUG, note: "BUG-008（症状1=code-defect + 症状2=req-gap，人确认）：MCP 服务行缺「编辑」+ 开关/pill 颜色不对。根因1：REQ-084 契约 CRUD 但 HTTP 面无 U（service.update 存在未暴露）+ UI 无编辑按钮，测试未覆盖（E2E 无编辑用例/API 直调 service）。根因2：已批准 UX HTML 本身缺陷被实现忠实复刻——.switch 无 display（span inline 塌缩，E2E 实测 2px 竖条）、.toggle-pill.on/.seg.active/.btn-tertiary:hover 三处 accent-text 白字-on-soft 不可读。就地补全 UX HTML（504ead8/addfb77 [ux]）+ 回归测试先红（9816c6f [test]，API 3 + E2E 2）+ 修复（5ca1c0a [bugfix]：PUT 路由/编辑按钮弹窗回填 name 只读 token 留空保留/全局 .switch inline-block/Plugins.css 三色）+ fixture 修正（1a58af2 [test]，X-Team→X_Team 合 KEY 契约）。API 11/11、E2E 11/11、全量单元 850/850 绿。同源残留（未修，提示另起 bug）：permission-mcp-group.html 的 .scope-seg.active/.override-tag accent-text 误用、Plugins.css .badge-npm 白字-on-soft"}
  - {at: 2026-08-15, from: BUG, to: BUG, note: "BUG-009（req-gap，人确认）：用户配了 MCP server 但 agent 报 server not found——配置层两缺口（名称笔误 firecawl / 未做项目启用），但「项目启用」pill 点了没反应是真缺陷：弹层 absolute 贴单元格向下展开，单行/末行整体超出 .plugin-section.section(overflow:hidden) 边界被裁剪。根因在已批准 UX HTML 弹层定位策略本身（原型多行从未触发末行出界），实现忠实复刻；另实现漏抄 HTML 的 click-away 关闭。就地补全（不退 phase）：UX 参照弹层改 fixed 视口定位+togglePop JS 注入坐标+重生成 preview（25fab9a [ux]）、E2E seam 先红（7b0b048 [test]，elementFromPoint 命中测试锚定真实可见——toBeVisible 不查祖先裁剪，实证红 hit=false）、修复（a33593b [bugfix]：Plugins.css fixed + Plugins.jsx togglePop 注入 popPos + 补 click-away，插件/MCP 两弹层同治）。E2E 12/12 绿（红→绿）。用户侧残留：firecawl 需删了重配 firecrawl + 弹层里对 knowledge 项目拨开关 + 项目空间新开会话"}
  - {at: 2026-08-15, from: BUG, to: BUG, note: "BUG-010（code-defect，人确认；跨 story 归属裁决=留本 story，REQ 属主 ux-enrichment/REQ-AGENT-047）：用户消息走 MarkdownRenderer，全局 .assistant-zone .md a 链接色=--ch-accent 与用户气泡实底（assistant.html 参照）同色→链接隐身。两份已批准参照冲突且均未覆盖「实色用户气泡内链接」组合（assistant-rich.html 用户气泡为 accent-soft 且无链接示例）。人拍板方向 A（气泡不动、链接继承白字）排除方向 B（气泡改浅底）。回归先红（0d4d14f [test]，E2E 计算色断言 link≠气泡底色∧=气泡文字色，实证 link=accent=底色）、修复（eb5a523 [bugfix]，.msg.user .bubble .md a{color:inherit} 一行）。红→绿；影响面审查：规则作用域 .msg.user，agent 气泡链接 accent 不受影响"}

# 归档记录。每次"归档重做"追加一条;删 story 不记(整个目录删除)。
archive: []

# 块状态(一挡内按块回流时维护)。
blocks: {}

# 设计系统绑定(由 /design-system 与 /ux-explore 维护)。
design_system:
  slug: "global"
  compiled: true
  preview: .aiassist/stories/2026-08-12-pi-mcp-plugin/ux/preview.html

# story 内已记录的设计资产(由 record-asset.mjs 维护)。
assets:
  recorded:
    - {path: ux/plugins-page.html, name: "插件管理页（插件清单 + MCP 服务）", status: needs-review}
    - {path: ux/permission-mcp-group.html, name: "权限配置 · MCP 规则族", status: needs-review}
    - {path: ux/oauth-present.html, name: "OAuth 授权呈现", status: needs-review}

# 已生成的变体(按需)。
variants:
  active: []
