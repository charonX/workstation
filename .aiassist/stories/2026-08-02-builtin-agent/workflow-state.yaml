# Test-as-Contract workflow state
# 由 /story 创建和维护。本文件是状态机,不归档(回流时更新,不搬进 archive/)。

story_id: 2026-08-02-builtin-agent
intention: >-
  平台的 AI 能力依赖本机 Claude Code 环境（新用户装完应用用不了）且只能手动搭 flow
  使用；人不在电脑前时无法了解平台正在进行的任务、无法下发任务——需要飞书对话驱动的
  内置 agent（零依赖内核 + 飞书入口，合并自 wayfind S1+S2）。

phase: BUG      # THINK / PRD / DESIGN / DOMAIN-MODEL / TECH-DESIGN / CRYSTALLIZE / TEST / ASSERTION-SIGNOFF / BUILD / QA / BUG / REFLECT
attempt: 1        # 第几次尝试。归档重做时 +1。
bug-counter: 4    # story 内 bug 计数，/bug 每次修复 +1，用于 commit [bugfix] BUG-NNN。
created: 2026-08-02

# 阶段流转历史。正常前进和回流都记一条。
history:
  - {at: 2026-08-02, from: -, to: THINK, note: "创建 story（wayfind builtin-agent 决议 S1+S2 合并：飞书入口即内核验证 seam，用户拍板）"}
  - {at: 2026-08-03, from: THINK, to: PRD, note: "demand-insight 完成（interview-notes），访谈拍板：供应商 DeepSeek+Kimi、确认挂起、方向 B 分层交付、身份配置"}
  - {at: 2026-08-03, from: PRD, to: TECH-DESIGN, note: "PRD v0.2（review 修复 F-1/F-2 + 拍板全部拒绝/agent 优先，REQ-CHANNEL-002 修订声明）"}
  - {at: 2026-08-03, from: TECH-DESIGN, to: CRYSTALLIZE, note: "tech-design v1（7 决策 A2/B1/C2/b/D1/E3/F1 拍板 + ADR-014）"}
  - {at: 2026-08-03, from: CRYSTALLIZE, to: TEST, note: "requirements v1（22 REQ + REQ-CHANNEL-002 接替声明 + H1~H4 假设）；CONTEXT.md 术语登记；agent-dialogue 能力入地图"}
  - {at: 2026-08-03, from: TEST, to: BUILD, note: "门 1 assertion-signoff 通过（20 项决策人拍板；spike H1~H4 前置验证；key 校验修订为非空+测试连接；pendingBind 10 分钟）"}
  - {at: 2026-08-04, from: BUILD, to: QA, note: "8/8 slice 完成，业务测试 516/516 全绿（含 3 次 code-defect 修复：多轮卡片/监听器累积/确认端点 404；PRD 对齐缺口全部闭环）；待 /qa-runner"}
  - {at: 2026-08-05, from: QA, to: BUG, note: "BUG-001 code-defect：主进程 stdio 无 EPIPE 防护——飞书消息 routeToAgent 失败处理路径 console.error 写断管 → uncaughtException 崩主进程（违反 REQ-AGENT-017 AC3 优雅降级）。修复：src/stdioGuard.js + main.js/headless-server.js 首行接线；回归测试先红后绿，全量 521/521"}
  - {at: 2026-08-05, from: BUG, to: BUG, note: "BUG-002 code-defect：vite.worker.config.js external 漏配——better-sqlite3 等 CJS 运行时依赖内联进 ESM bundle，bare require 走 __require shim → worker import 期即崩 → 看门狗报「反复崩溃」（违反 REQ-AGENT-005 AC1 spawn→ready 打包形态）。修复：external 补齐 5 依赖（regex 含子路径）；回归测试走 forge 管线保真构建 + spawn 断言 ready，先红后绿，全量 523/523"}
  - {at: 2026-08-05, from: BUG, to: BUG, note: "BUG-003 not-a-bug 关闭（人拍板）：「保存后重启 provider 配置没有了」实为签核决策 5 设计行为——key 永不回显，徽章「已配置 DeepSeek」即持久化指示；证据：settings.json agent 区完整、读路径忠实、渲染层无 agent clobber。空 KEY 框 ≠ 配置丢失，不修代码。同场登记 UX 诉求：Settings 页 tab 化 + 独立保存（去右上角全局保存）→ 走 /design 设计迭代"}
  - {at: 2026-08-05, from: BUG, to: CRYSTALLIZE, note: "Settings tab 化 UX 原型拍板 approved（ux/settings-tabs.html 四 tab + 分区保存 + 去全局保存）；C.10 收割：PRD 稳定块 S10 落入。压缩后续接：/crystallize 把 S10 结晶为 REQ 增量 → /test-author 补测试（tab 切换/分区保存/无全局保存）→ 增量断言签核 → /implementer 改 Settings.jsx → /qa-runner"}
  - {at: 2026-08-05, from: CRYSTALLIZE, to: CRYSTALLIZE, note: "S10 结晶为 REQ 增量 REQ-AGENT-023~025（tab 化结构 / 分区独立保存去全局保存 / 切换保留未保存编辑；P1 应该，E2E seam=settingsTabs.test.cjs）；完整性审查通过（错误码/验证规则复用现有，无 gap report）；hash 重算 16f30c7b；能力地图 settings 行更新。待人确认 REQ 增量后 /test-author"}
  - {at: 2026-08-05, from: CRYSTALLIZE, to: TEST, note: "REQ 增量人确认；test-author 完成：settingsTabs.test.cjs 11 例（tab 结构/文案/切换/分区归属/分区保存请求体隔离/keepExistingKey/通道保存 mock/失败区内显示/编辑保留/零变更请求）；测试侧接替适配 themeLanguage/onboarding/versionDisplay 三签名套件（导航适配，断言语义不变）；agent-dialogue REQ-VERSION 头扫到 16f30c7b。待增量断言签核（en-US tab 译文为待签占位）"}
  - {at: 2026-08-05, from: TEST, to: BUILD, note: "门 1 增量断言签核通过（3 裁决：11 例全签 / en-US 译文授权 i18n 直译入 REFLECT / 三套件导航适配免重签）；signoff.md 增量回写 + 实现者测试缝契约（tab 属性契约 + save-general-settings-button 新增 + save-settings-button 移除）。进 /implementer 改 Settings.jsx"}
  - {at: 2026-08-05, from: BUG, to: BUG, note: "BUG-004 code-defect（人拍板 code-defect + 最小修复先解锁）：BUG-002 worker 构建修复后启动不报错，但飞书发消息无回复。根因：agentService DEFAULT_MODELS 硬编码 deepseek-chat / kimi-latest 在 pi 运行时模型目录不存在（deepseek provider 仅 deepseek-v4-flash/deepseek-v4-pro，moonshotai 仅 kimi-k2.x/kimi-k3）→ worker resolveModel 抛 E-AGENT-MODEL → session-config 失败 → 会话建不起来 → 无回复（违反 REQ-AGENT-006 AC1）。修复：DEFAULT_MODELS 改 deepseek-v4-flash / kimi-k2.5 + 导出供测试 seam；回归测试 agentDefaultModel.test.js 以 resolveModel 同构 seam（ModelRuntime.getModel）先红后绿；worker 级复现 config-ack 验证；全量 525/525。模型配置化（供应商拉取列表选默认模型）登记为后续 feature"}

# 归档记录。每次"归档重做"追加一条;删 story 不记(整个目录删除)。
archive: []
#  - {attempt: 1, date: 2026-07-03, reason_file: archive/attempt-1/reason.md, root_layer: PRD}
#    root_layer 取值:THINK / PRD / DESIGN / DOMAIN-MODEL / TECH-DESIGN —— 表示这次回流判定根因活在哪一层。

# 块状态(一挡内按块回流时维护)。
blocks: {}
#  stable: [块A, 块B]      # 已稳定,可结晶为 REQ
#  moving: [块C]           # 还在动,暂不入 REQ(被推翻的块降级回这里)

# 设计系统绑定(由 /design-system 与 /ux-explore 维护)。
design_system:
  slug: "global"            # 主设计系统 slug,对应 _d_meta.json 的 primaryDesignSystem
  compiled: true            # 是否已跑过 compile-design-system
  preview: ".aiassist/stories/2026-08-02-builtin-agent/ux/preview.html"               # 当前有效的 preview.html 路径

# story 内已记录的设计资产(由 record-asset.mjs 维护)。
assets:
  recorded: [settings-tabs.html]              # [flow-a.html, flow-b.html, ...]

# 已生成的变体(按需)。
variants:
  active: []                # [dark, compact, ...]
