story_id: 2026-07-16-flow-refinement
story_name: Flow 细化
intention: 当前 flow 框架只支持最基础的节点与执行路径，面对真实自动化场景（错误处理、重试、变量作用域、可视化编排体验等）能力不足，需要系统性地细化 flow 的节点类型、执行语义和编辑器体验。
current_phase: BUILD
completed: false
attempt: 1
bug_counter: 0
blockers: []
slices: []
gates:
  assertion_signoff: passed
  feel_signoff: pending
history:
  - {at: 2026-07-16, from: THINK, to: PRD, note: "完成需求洞察，生成 PRD"}
  - {at: 2026-07-17, from: PRD, to: CRYSTALLIZE, note: "PRD 审查问题已修复，结晶为 REQ-FLOW-018~028"}
  - {at: 2026-07-17, from: CRYSTALLIZE, to: TEST, note: "test-author 生成 13 个测试骨架，等待 assertion-signoff"}
  - {at: 2026-07-17, from: TEST, to: ASSERTION-SIGNOFF, note: "用户签核全部 59 处断言，assertion-signoff 通过，解锁 BUILD"}
  - {at: 2026-07-17, from: ASSERTION-SIGNOFF, to: BUILD, note: "补档 TECH-DESIGN 完成（本 story PRD→CRYSTALLIZE 时跳过，signoff 遗留 3 项 + 移动块 6 项在本阶段决策）：Agent SDK 选定 @anthropic-ai/claude-agent-sdk（ADR-005）；凭证不存储、复用本机 claude code；config.options 透传 allowlist（systemPrompt/maxTurns），面板不扩展；日志清理 purgeExpiredExecutions 滚动 7×24h，server 启动 + 每日 cron 双触发；触发方式维持节点外模型，webhook 与手动变量表单划入范围外（tech-design §4.3）。修复 10 个测试文件 vitest→node:test 框架错配（[test] 560faca，41 测试可运行：19 绿/22 红色契约）。tech-design.md 经用户审查定稿，解锁 BUILD。"}
archive: []
notes: |
  继承自 codex-harness-desktop 已沉淀的 FlowEngine 基础：
  - 引擎为自建 FlowEngine，进程内执行 agent/computer-use。
  - 允许循环图；Condition/ForEach/While 为 MVP 基础节点。
  - 通过 maxDepth/maxIterations 防止无限循环。
  - 表达式使用 JavaScript。
  本 story 重点是对这些基础能力做细化与扩展。
  签核决策：校验放 service 层；错误格式结构化；Condition 用 fullName，Agent prompt 用 {{fullName}}；mock 通过 options.executors 注入；新建 execution_nodes 表；E2E 只签核行为级断言。
  tech-design 决策（2026-07-17 补档）：Agent SDK = @anthropic-ai/claude-agent-sdk（ADR-005）；凭证复用本机 claude code，应用不存储；config.options 仅透传 systemPrompt/maxTurns；日志清理 = purgeExpiredExecutions 滚动 7×24h，server 启动 + 每日 cron 双触发；触发方式在节点外（手动=debug endpoint，定时=schedules 实体），webhook 与手动变量表单为范围外。BUILD 第一切片：packaged 形态 SDK 子进程 spike。
