# Test Plan: OPC Workstation Desktop App

> story: `codex-harness-desktop`  
> attempt: 3  
> generated by: `/test-author`  
> requirements-v1.hash: `53fcb918ad26820e6760c66ac610791ceca2a11a981737c76234a70ea8f36569`

---

## 1. 测试分层

| 层级 | 占比 | 工具 | 目录 |
|---|---|---|---|
| 单元/服务测试 | ~80% | Node.js built-in test runner | `tests/capabilities/*/api/*.test.js` |
| 集成测试（CLI/HTTP） | ~15% | Node.js built-in test runner + execSync | `tests/capabilities/*/api/*.test.js`, `tests/capabilities/*/cli/*.test.js` |
| 浏览器 E2E | ~5% | Playwright + Electron | `tests/capabilities/*/e2e/*.test.js` |

---

## 2. E2E 覆盖矩阵

| E2E 文件 | 覆盖 REQ-ID | 关键用户路径 | 数据准备 | 纯审美部分（留给 feel-signoff） |
|---|---|---|---|---|
| `workspace-management/project/e2e/onboarding.test.js` | REQ-WORKSPACE-003~006, REQ-WORKSPACE-007, REQ-I18N-001~002 | Settings → Add Project → Configure Skills；Theme/Language/Density 切换 | 从零开始（除 density 外） | 设置表单排版、主题暗色/亮色视觉气质 |
| `flow-orchestration/flow/e2e/flowRun.test.js` | REQ-FLOW-002~005, REQ-SCHEDULE-001, REQ-SCHEDULE-003 | Create Flow → Edit Nodes → Run → View Execution | `beforeEach` API seed 一个 project | React Flow 节点样式、动效、画布缩放动画 |
| `flow-orchestration/flow/e2e/flowEditor.test.cjs` | REQ-FLOW-013~017 | Flow Editor 节点删除、节点专属配置持久化、Condition true/false 双输出、Publish/Draft 状态、Debug 弹层 | `beforeEach` API seed 一个 project | 删除按钮样式、Condition handle 视觉位置、Properties 面板排版、Publish/Debug 按钮视觉 |
| `skill-management/skill/e2e/skillInstall.test.cjs` | REQ-SKILL-002, REQ-SKILL-003 | Install Skill → View Detail；验证 local 安装后源目录被复制到 `skillRepoPath/<name>`，且根目录存在 `SKILL.md` | 从零开始 | 弹层动效、Skill README 排版 |
| `internationalization-theme/theme/e2e/themeLanguage.test.js` | REQ-I18N-001, REQ-I18N-002 | Theme / Language 切换 DOM 效果 | 从零开始 | 颜色 token 的视觉表达、字体 |
| `information-aggregation/dashboard/e2e/dashboard.test.js` | REQ-DASH-001 | Dashboard 指标渲染 | `beforeEach` API seed project/flow/execution | 卡片布局、间距、动效 |

---

## 3. 来自 HTML UX 原型的自动化映射

| UX 原型 | 提取的可验证项 | 对应 E2E 测试 |
|---|---|---|
| `settings.html` | Settings 表单字段存在与保存；theme/language/density 控件 | `onboarding.test.js`, `themeLanguage.test.js` |
| `workspace-home.html` | Add Project 按钮/弹层；Project Card；Configure Skills 入口 | `onboarding.test.js` |
| `flows.html` | New Flow 按钮/弹层；Flow Card | `flowRun.test.js` |
| `flow-editor.html` | Node Palette 分类；画布；Properties 面板；Run 按钮；Zoom 控件；Condition 节点双输出 port；节点配置字段 | `flowRun.test.js`, `flowEditor.test.cjs` |
| `executions.html` | Executions 列表；Execution Detail Logs/Variables/Output | `flowRun.test.js` |
| `skills.html` | Install Skill 按钮/弹层；Skill Table；Skill Detail Tab | `skillInstall.test.js` |
| `dashboard-overview.html` | 指标卡片；最近执行列表；快捷项目入口 | `dashboard.test.js` |

---

## 4. 占位断言与 TODO

以下位置使用 `// TODO: HUMAN ASSERTION`，需要人在 `/signoff --stage=assertion` 前确认预期值：

1. `tests/e2e/fixtures/electronApp.js` — Electron 启动入口与 `server.json` 轮询策略。
2. `tests/e2e/helpers/seed.js` — API seed 请求体字段。
3. `onboarding.test.js` — 本地文件路径、项目名、关联 skill 名称。
4. `flowRun.test.js` — project id 回填、节点添加方式、zoom 可测量断言。
5. `flowEditor.test.cjs` — 节点删除触发方式（按钮/Delete 键）、各节点类型字段标签、Condition handle 的 data-handleid 值、持久化后的字段回显值。
6. `skillInstall.test.js` — npm/local skill 标识符、README 内容断言、安装后 `repoPath` 位于 `skillRepoPath` 下且存在 `SKILL.md`。
6. `themeLanguage.test.js` — 中英文 UI 文案断言。
7. `dashboard.test.js` — 指标数值、快捷链接跳转目标。

---

## 5. 基础设施变更

- 新增 `playwright.config.js`（root）。
- `package.json` 新增 devDependency `@playwright/test` 与 npm script `test:e2e`。
- CI `.github/workflows/contract-gate.yml` 已包含 Playwright 浏览器安装与 `npm run test:e2e`。

---

## 6. feel-signoff 范围

以下判断**不进入自动化测试**，仅在 `/signoff --stage=feel` 依据 HTML UX 原型验收：

- 颜色、间距、字体、排版、圆角、阴影。
- 弹层出现/消失动效、画布平移动效、按钮悬停动效。
- React Flow 节点与连边的具体视觉样式（与手绘原型的差异已在 `tech-design.md §9.5` 记录）。

---

## 7. 已知限制与待实现

- 部分 seed 请求体需要实现完成后的真实字段确认。

---

## 8. 既有 API/CLI 测试

以下测试来自 attempt-2，本轮更新 `flow.test.js` 以覆盖 REQ-FLOW-016/017，并更新 `flowEditor.test.cjs` REQ-TRACE 与 E2E 覆盖：

- `workspace-management/settings/codex-harness-desktop/api/settings.test.js`
- `workspace-management/project/codex-harness-desktop/api/project.test.js`
- `flow-orchestration/flow/codex-harness-desktop/api/flow.test.js`
- `flow-orchestration/flow-engine/codex-harness-desktop/api/flowEngine.test.js`
- `scheduling-execution/task/codex-harness-desktop/api/task.test.js`
- `scheduling-execution/schedule/codex-harness-desktop/api/schedule.test.js`
- `skill-management/skill/codex-harness-desktop/api/skill.test.js`
- `information-aggregation/dashboard/codex-harness-desktop/api/dashboard.test.js`
- `internationalization-theme/language/codex-harness-desktop/api/language.test.js`
- `internationalization-theme/theme/codex-harness-desktop/api/theme.test.js`
- `command-interface/cli/codex-harness-desktop/cli/cli.test.js`
