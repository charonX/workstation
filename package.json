{
  "name": "opc-workstation",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/main/main.js",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "dev": "vite",
    "build": "vite build",
    "electron": "electron src/main/main.js",
    "start": "npm run build && npm run electron",
    "rebuild:electron": "CXXFLAGS='-std=c++20' npx @electron/rebuild -f -w better-sqlite3",
    "rebuild:node": "npm rebuild better-sqlite3"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "electron": "^43.0.0",
    "node-cron": "^4.5.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.1.0",
    "@vitejs/plugin-react": "^6.0.3",
    "electron-rebuild": "^3.2.9",
    "vite": "^8.1.3"
  }
}
